import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF1565C0);
  static const primaryDark = Color(0xFF0D47A1);
  static const primaryLight = Color(0xFF1976D2);
  static const accent = Color(0xFFFF6F00);

  // Priority
  static const priorityCritical = Color(0xFFC62828);
  static const priorityCriticalBg = Color(0xFFFFEBEE);
  static const priorityHigh = Color(0xFFE65100);
  static const priorityHighBg = Color(0xFFFFE0B2);
  static const priorityMedium = Color(0xFFF57F17);
  static const priorityMediumBg = Color(0xFFFFF9C4);
  static const priorityLow = Color(0xFF2E7D32);
  static const priorityLowBg = Color(0xFFE8F5E9);

  // Status
  static const statusReported = Color(0xFF1565C0);
  static const statusInProgress = Color(0xFFE65100);
  static const statusResolved = Color(0xFF2E7D32);

  // UI
  static const background = Color(0xFFF0F2F5);
  static const surface = Colors.white;
  static const cardBg = Colors.white;
  static const divider = Color(0xFFE0E0E0);

  // Dark gradient
  static const gradientStart = Color(0xFF0D47A1);
  static const gradientEnd = Color(0xFF1565C0);

  // Text
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textLight = Color(0xFF9CA3AF);

  // Dashboard
  static const dashBg = Color(0xFF0D1B2A);
  static const dashCard = Color(0xFF1B2A3B);
  static const dashCardLight = Color(0xFF243447);
}
